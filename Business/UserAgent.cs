using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Mercoplano.Maisbarato.Server.RESTful.Business
{
    public class UserAgent
    {
        public String setPasswordReset(string email)
        {
            /*
            String password = getPassword();
            String passwordMD5 = Util.CriarHasMD5(password);
            DAL.SQLServer.UserDAO.setPasswordReset(passwordMD5, email);

            return password;
            */
            return null;
        }

    }
}
